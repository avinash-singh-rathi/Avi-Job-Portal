#.htaccess file for the security as well as for the url redirection
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /                           #Path set to root
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]            #Path to redirect the file
</IfModule>